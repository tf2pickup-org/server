import { SerializerInterceptor } from './serializer.interceptor';

describe('SerializerInterceptor', () => {
  it('should be defined', () => {
    expect(new SerializerInterceptor()).toBeDefined();
  });
});
