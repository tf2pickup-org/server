import { UnderscoreIdToIdInterceptor } from './underscore-id-to-id.interceptor';

describe('UnderscoreIdToIdInterceptor', () => {
  it('should be defined', () => {
    expect(new UnderscoreIdToIdInterceptor()).toBeDefined();
  });
});
