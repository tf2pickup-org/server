import { ConfigurationEntryErrorFilter } from './configuration-entry-error.filter';

describe('ConfigurationEntryErrorFilter', () => {
  it('should be defined', () => {
    expect(new ConfigurationEntryErrorFilter()).toBeDefined();
  });
});
