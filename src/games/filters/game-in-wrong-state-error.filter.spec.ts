import { GameInWrongStateErrorFilter } from './game-in-wrong-state-error.filter';

describe('GameInWrongStateErrorFilter', () => {
  it('should be defined', () => {
    expect(new GameInWrongStateErrorFilter()).toBeDefined();
  });
});
