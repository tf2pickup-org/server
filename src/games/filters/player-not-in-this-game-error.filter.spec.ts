import { PlayerNotInThisGameErrorFilter } from './player-not-in-this-game-error.filter';

describe('PlayerNotInThisGameErrorFilter', () => {
  it('should be defined', () => {
    expect(new PlayerNotInThisGameErrorFilter()).toBeDefined();
  });
});
