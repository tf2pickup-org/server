import { ParseSortParamsPipe } from './parse-sort-params.pipe';

describe('ParseSortParamsPipe', () => {
  it('should be defined', () => {
    expect(new ParseSortParamsPipe()).toBeDefined();
  });
});
