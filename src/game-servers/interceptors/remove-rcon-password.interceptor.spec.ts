import { RemoveRconPasswordInterceptor } from './remove-rcon-password.interceptor';

describe('RemoveRconPasswordInterceptor', () => {
  it('should be defined', () => {
    expect(new RemoveRconPasswordInterceptor()).toBeDefined();
  });
});
